machine:
  xcode:
    version: 8.2

dependencies:
  pre:
    - gem install xcpretty --no-rdoc --no-ri --no-document --quiet
    - pod setup
  cache_directories:
    -  "~/.cocoapods/repos/master"

test:
  override:
    - rake test
    - rake lint
